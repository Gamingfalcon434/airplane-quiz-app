import React from 'react';
import ReactDOM from 'react-dom/client';
import USFighterJetQuiz from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <USFighterJetQuiz />
  </React.StrictMode>
);
